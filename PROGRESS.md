# PROGRESS

Living status tracker. Rule: update this file **in the same commit** as the work it describes. Checklists mirror [INSTRUCTIONS.md](INSTRUCTIONS.md); details/rationale in [PLAN.md](PLAN.md).

**Legend:** `[ ]` pending · `[x]` done · status per phase: `not started | in progress | done`

---

## Pre-Phase — Planning & Repo Setup   [status: done]  (2026-07-16)
- [x] Source files analyzed (xlsm formulas, 3 Word drafts, attendance format extracted & verified)
- [x] Stack + business decisions confirmed with owner (see PLAN §2)
- [x] PLAN.md, INSTRUCTIONS.md, PROGRESS.md written
- [x] git init; source files moved to `docs/source/`
- [x] Complete folder structure created (backend/frontend/docs/scripts, all module folders)
### Notes / deviations
- 2026-07-16: Code scaffolding intentionally deferred — user instructed planning files + folder structure only.
- 2026-07-17: Correction — `git init` had **not** actually been run as of 2026-07-16 despite the checklist above; the repo was initialized today and the Phase 0 scaffold is its root commit.

## Phase 0 — Scaffolding & Dev Environment   [status: done]   (2026-07-17)
- [x] Root meta files (.gitattributes, .editorconfig, .env.example)
- [x] scripts/check_line_limits.py + check_all.ps1/.sh
- [x] Backend: uv project + FastAPI /health + SQLAlchemy base + Alembic wired
- [x] Backend Dockerfile (dev+prod targets) + entrypoint.sh (LF)
- [x] Frontend: Vite React TS + antd + i18next EN/AR + RTL flip + AppShell
- [x] Frontend Dockerfile + nginx.conf (/api proxy)
- [x] docker-compose.yml + docker-compose.dev.yml
- [x] CI workflow + pre-commit hooks
- [x] docs/ skeleton files
- [x] DEMO GATE: compose up boots bilingual stack; all gates green
### Notes / deviations
- 2026-07-17: Docker Desktop + WSL2 installed on the dev machine (were missing). Full demo gate verified end-to-end:
  - `docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build` → `GET /health` on :8000 returns `{"status":"ok","db":"connected"}` against the real postgres container; frontend on :5173 serves the Arabic RTL shell.
  - `docker compose up --build` (prod-shaped, single-origin) → frontend on :80 serves the RTL shell via nginx; `/api/v1/*` proxies through to the backend (confirmed via a live FastAPI 404, not an nginx gateway error); backend `/health` (not published, checked via `docker exec`) returns `db:"connected"`.
  - Fixed two real bugs surfaced only by the actual Docker build (not caught by bare-metal `uv run`): (1) `backend/Dockerfile` ran `uv sync` before `app/__init__.py` was copied into the image — split into a cached deps-only `uv sync --no-install-project` layer followed by the real sync after `COPY`; (2) the prod stage didn't copy `README.md`, which `pyproject.toml`'s `readme =` field requires at build time.
- 2026-07-17: `uv init` initially created a nested git repo + `src/` package layout inside `backend/`; removed and rebuilt to the flat `app/` layout INSTRUCTIONS.md specifies.
- 2026-07-17: docs/ skeleton written per PLAN §10 (architecture, database-schema, api-reference, calculation-engine, user-roles, workflows, attendance-import, kpi-templates, deployment, development, i18n-guide, ADR template). **`docs/source/` is empty** — the real workbook/Word/attendance files PLAN.md references (`Precast Incentives 03-2026 (1).xlsm`, 3 Word drafts, Oracle attendance export) are not actually in the repo yet; needed before Phase 2/7 can proceed for real.
- 2026-07-17: docs/ skeleton files (architecture.md, database-schema.md, etc. per PLAN §10) deferred — not blocking the demo gate, will add alongside the content they document.

## Phase 1 — Auth, Users, RBAC   [status: done]   (2026-07-18)
- [x] users/roles/user_roles/refresh_tokens models + migration
- [x] bcrypt + JWT + refresh rotation (httpOnly cookie)
- [x] login / refresh / logout / me / change-password endpoints
- [x] require_roles dependency + HR-only role grants (admin excluded)
- [x] audit_log table + write_audit core, wired to auth events
- [x] seed: 9 roles + admin user (forced password change)
- [x] Frontend: login, authStore, guards, permissions.ts, Can, users admin screen
- [x] Tests: security unit + auth API + interceptor refresh queue
- [x] DEMO GATE: role-gated 403/200 + audit rows
### Notes / deviations
- 2026-07-18: **Real bug caught by manual testing**: the refresh cookie was originally scoped to `path=/api/v1/auth/refresh` per INSTRUCTIONS.md's literal wording — but a cookie scoped to one exact path is never sent by a browser (or curl, correctly) to a sibling path, so `/auth/logout` could never actually read it and silently did nothing while still returning 204. Fixed by widening the cookie's `path` to `/api/v1/auth` (still narrower than site-wide `/`, covers `refresh` + `logout`). Caught only because the full login→refresh→logout→replay flow was walked end-to-end with real cookies, not just unit-tested in isolation.
- 2026-07-18: `users.employee_id` is a nullable `BigInteger` with **no FK constraint yet** — the `employees` table doesn't exist until Phase 2, which will add the constraint via a new migration. Documented inline in `models.py`.
- 2026-07-18: Seed bootstrap: admin user `0001` is granted **both** `admin` and `hr` roles directly at the DB layer in `seed.py` (bypassing the runtime rule that only HR can grant roles, which not even admin can bypass through the API). This is the intentional, documented way the very first HR account gets created — someone has to be able to grant roles before any HR account exists. Default password `ChangeMe123!`, `must_change_password=true`.
- 2026-07-18: Permission split for `/users/*` wasn't fully spelled out in INSTRUCTIONS.md ("Users admin: GET/POST/PATCH /users, PUT /users/{id}/roles (HR only), POST reset-password (HR)") — interpreted as: list/create/patch-active open to **ADMIN or HR**, role assignment and password reset **HR only**. Worth confirming with PMO/HR if this split matches intent.
- 2026-07-18: **Dev-machine-only quirk, not a project issue**: this machine has a pre-existing native Windows PostgreSQL 18 service bound to `localhost:5432`, which silently intercepts any bare-metal (`uv run alembic`/`uv run pytest`) connection attempt meant for the Docker Postgres. Migrations, seeding, and pytest were all run via `docker exec ems-backend-1 uv run ...` instead, which correctly resolves the `postgres` hostname on the compose network. CI is unaffected (no such conflict on GitHub Actions runners).
- 2026-07-18: Test DB isolation: `tests/conftest.py` creates/reuses a separate `ems_test` database and truncates all tables after each test (not the transaction-rollback/savepoint pattern) — simpler to reason about, fine at this data volume.
- 2026-07-18: `openapi-typescript` codegen isn't wired up yet — `frontend/src/modules/auth/types.ts` is hand-written to mirror the Pydantic schemas (snake_case, per PLAN §7). Fine per plan ("hand-written thin API fns"), but the committed-schema-snapshot pipeline itself is still a Phase 0 TODO if we want it later.
- 2026-07-18: Verified the full backend flow live via curl against the real Docker Postgres (not just pytest): login → forced password change → create user → assign role as HR → 403 for admin/reviewer attempting HR-only actions → audit rows in DB → refresh rotation revokes the old token → logout revokes the current token.
- 2026-07-18: Owner then click-tested the real login page in a browser and hit **four further real bugs** invisible from curl-only testing, all now fixed:
  1. Password/staff-number `<Input>`s had no `dir="ltr"`, so the RTL page bidi-reordered typed Latin+punctuation text (a trailing `!` visually jumped to the front) — confusing to type in and to verify. First fix (`dir="ltr"` on the component) was incomplete: antd's `Input.Password` renders through an affix wrapper because of its visibility-toggle icon, so `dir`/`style` landed on the wrapper, not the real `<input>`. Fixed properly with antd's `styles={{ input: {...} }}` semantic slot (`shared/ui/ltrInput.ts`).
  2. `navigate()` calls after login/logout into a lazy-loaded route (`React.lazy` + Suspense) threw "component suspended while responding to synchronous input" under React 18's stricter Suspense rules. Fixed by wrapping those specific `navigate()` calls in `React.startTransition(...)` directly (LoginPage, ChangePasswordPage, AppShell logout).
  3. **The big one**: Vite's dev-server `/api` proxy target was hardcoded to `http://localhost:8000`. That only resolves inside the *frontend* container's own network namespace when running the dockerized dev stack — nothing listens there, since the backend is a separate container. Every real browser login attempt was silently failing at the proxy layer. Made the target configurable via `VITE_API_PROXY_TARGET`, set to `http://backend:8000` in `docker-compose.dev.yml`. This one had been broken since Phase 0 and wasn't caught earlier because prior verification always curled the backend's published port directly rather than going through the frontend's own proxy path.
  4. LoginPage/ChangePasswordPage always displayed "invalid password" on *any* failure, including the proxy bug above — actively misleading during diagnosis. Added `shared/utils/apiError.ts` to distinguish network errors, known backend error codes, and unknown failures with distinct messages.
  - **Lesson for future phases**: curling the backend's published port (`localhost:8000`) proves the API works; it does **not** prove the frontend can reach it through its own dev-server proxy when containerized. Test end-to-end through the frontend's own port (`localhost:5173/api/...`) going forward, per the `verify` skill's intent.

## Phase 2 — Org Structure   [status: done]   (2026-07-19)
- [x] departments/positions/position_rates/employees/employee_salaries models (+ btree_gist, pg_trgm) + migration `1861e1738425`
- [x] seed JSON: 9 departments (see Engs note below) + 46 positions with flat rates (`Database!F2:G47` is 46 rows, not 47 — PLAN's "47" was an off-by-one against the actual range)
- [x] as-of helpers (`rate_as_of`, `salary_as_of`, first-of-month rule) + boundary tests
- [x] org + employees + salaries endpoints (scoped, salary role-gated) — pre-existing from the prior commit, reviewed
- [x] `backend/scripts/import_legacy.py` — parses all 9 real department sheets header-name-first (never fixed column indices: the real sheets disagree with each other on column order and some duplicate the "Actual Position Final" header), resolves positions, applies the "خروج نهائي" (final exit) marker as the only free-text phrase that means terminated, backfills `full_name_en` from the attendance export by staff number, reports every unresolved row instead of skipping silently. Run for real against the actual `docs/source/*.xlsm`: **440 employees created**, 3 rows correctly reported unresolved (genuinely blank "Actual Position Final" cells in the source), 407 got `full_name_en` backfilled from the June-2026 attendance export (33 have no match there — plausible for a March-2026 roster vs a later attendance month). Re-ran the import a second time: 0 created / 440 updated — confirmed idempotent.
- [x] Frontend: org module (departments/positions tabs, rate-history modal), employees module (searchable roster via `DataTable`, detail page with overview/edit, reviewer assignment, salary tabs), `useLocalizedField` hook — all under `MANAGE_ORG`/`MANAGE_EMPLOYEES`/`VIEW_SALARY` permissions
- [x] OPEN QUESTION answered: `Engs` is seeded and imported as its own (9th) department (code `ENGS`), not a cross-department tag — matches how the seed data was already structured
- [x] DEMO GATE: real roster EN/AR browsable; salary hidden from Reviewer — **verified live**, see below
### Notes / deviations
- 2026-07-19: All static gates are green (`ruff check`, `mypy app`, `pytest`, frontend `tsc -b`, `eslint`, `vitest run`, `vite build`, and the repo's line-limit script) — run both bare and inside the actual `ems-backend-1`/`ems-frontend-1` containers. Fixed two real `mypy --strict` failures in the already-committed `org/service.py` and `employees/service.py` (untyped `Decimal`/`date` params) while touching those files.
- 2026-07-19: **Real bug caught by the new tests, not by manual testing this time**: `app/main.py`'s custom `RequestValidationError` handler passed `exc.errors()` straight into `JSONResponse`, but pydantic's `@model_validator(mode="after")`-raised `ValueError`s land inside `ctx.error` as the *raw exception object*, which `json.dumps` can't serialize — so any 422 from a custom validator (e.g. `PositionRateCreateRequest`'s "at least one of incentive_pct/flat_ref_amount" check) crashed as an unhandled 500 instead of returning 422. Fixed with a `_sanitize_errors` pass that stringifies `ctx.error` before `jsonable_encoder`. Only surfaced because `test_position_rate_requires_at_least_one_rate_value` was the first test in the repo to actually exercise a model-level (not field-level) validator through the API.
- 2026-07-19: Docker Desktop's WSL2 backend went unresponsive mid-session (`docker ps` hung indefinitely, survived a Docker Desktop process restart + `wsl --shutdown`); recovered after the machine was rebooted. Once back up, ran the full sequence for real: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build` → `alembic upgrade head` (entrypoint, confirmed at `1861e1738425`) → `seed.py --core` → `import_legacy.py` (twice, to prove idempotency) → full `pytest` (45 passed) → live demo gate over HTTP through the frontend's own dev proxy (`localhost:5173/api/...`, not the backend's published port directly, per the Phase 1 lesson): logged in as HR/admin, listed all 440 employees with real bilingual names, created a `reviewer` test user, confirmed 403 on an employee they weren't assigned to, 200 after HR assigned them, then **403 on that same employee's salaries** even though they could see the employee record — proving the salary gate is a real, separate authorization check and not just record-level scoping. Test reviewer user deactivated afterward, not deleted (no hard deletes on domain data, per PLAN §5).
- 2026-07-19: Mounted `./docs:/docs:ro` into the dev backend container (`docker-compose.dev.yml`) so `import_legacy.py` can reach the real source files from inside the container; invoked with `MSYS_NO_PATHCONV=1` on Windows to stop Git Bash from mangling the `/docs/...` container path into a host path.

## Phase 3 — KPI Templates   [status: done]   (2026-07-20)
- [x] `kpi_templates`/`kpi_template_versions`/`kpi_criteria`/`kpi_template_assignments` models + migration `c77cd59823ee`. Version lifecycle draft→active→archived (partial unique index: at most one active version per template); assignments are temporal (GiST EXCLUDE, same pattern as `position_rates`/`employee_salaries`).
- [x] Lifecycle rules in `kpi_templates/service.py`: criteria only editable while `draft` (`_require_draft` guard on create/patch/delete criterion); `activate_version` archives the template's previous active version and enforces the sum rule; `clone_version` copies an existing version's criteria into a new draft (defaults to cloning the current active version).
- [x] Sum-to-100 validator enforced server-side at activation (`criteria_sum_invalid` 400 if criteria are missing or don't sum to exactly 100); mirrored client-side via a live `Progress` indicator on the version's criteria table that greys out the Activate button until the total is exactly 100.
- [x] Seeded all 4 real templates, pre-activated as v1 (`app/db/seed_data/kpi_templates.json`, wired into `seed.py --core`):
  - SKILLED / NON_SKILLED / KEY_FOREMAN — criteria, marks, and guidance text transcribed from PLAN §3.2 (extracted from the 3 Word evaluation drafts, no separate re-extraction needed since PLAN's table is already the verified source of truth). Key Foreman's "Worked Beyond Duty Hours" is 20 pts (not 30, unlike the other two) and it has 8 criteria vs 7 — both confirmed against PLAN's table, not assumed.
  - LEGACY_TEAM — the 8-criterion 30/20/10/10/10/10/5/5 scheme, `input_mode=scale_1_5`. Re-derived directly from the real workbook this time (not just trusted from PLAN) by reading the actual KPI column headers off all 8 non-Engs department sheets in `docs/source/Precast Incentives 03-2026.xlsm` — identical criteria set on every one of them (Production Efficiency, Quality Control, Safety Performance, Attendance, Teamwork, Skill Development, Equipment Handling, Communication).
  - **Noted but intentionally not built**: `Engs` uses a *different* 8-criterion legacy set (Planning/Reporting/Timelines, Cost Efficiency, Safety instead of some of the standard ones — still 30/20/10/10/10/10/5/5). INSTRUCTIONS.md Phase 3 scope is explicitly 4 templates; adding a 5th `ENGS_LEGACY_TEAM` was out of scope for this phase and would need a product decision on whether Engs should get its own template or use LEGACY_TEAM regardless. Flagging for Phase 5/7 — currently no template is assigned to Engs positions in seed data.
- [x] Endpoints: template CRUD + versions + clone + activate + criteria CRUD (PMO/ADMIN write, any authenticated role can read), position↔template assignments (PMO/HR write, overlap-rejected 409, same as Phase 2's rate/salary windows).
- [x] Frontend (`kpi-templates` module): template list with active-version badge, detail page with a version-tab history view, per-version criteria table (inline max-marks edit + add/delete while draft), live sum-to-100 circular progress + gated Activate button, Clone-to-new-draft button. Position↔template assignment UI added to the existing Org → Positions screen (a "KPI Template" button per position opens an assignment-history + add-assignment modal), rather than a separate screen — kept the "manage a position's stuff" affordances in one place.
- [x] Tests (`test_kpi_templates.py`, `test_kpi_templates_seed_data.py`): sum-to-100 rejection and acceptance, activation archiving the previous active version, draft-only edit enforcement (create/patch/delete all 400 on non-draft), clone copies criteria without mutating the source, assignment overlap 409, as-of resolution boundaries, RBAC (template writes PMO/ADMIN-only, assignment writes PMO/HR-only, reads open).
- [x] DEMO GATE: **run live** against the real Docker stack, not just curl-simulated — see notes.
### Notes / deviations
- 2026-07-20: **Real bug caught by a new test, fixed**: `app/main.py`'s `RequestValidationError` handler passed `exc.errors()` straight into `JSONResponse`; pydantic's `@model_validator(mode="after")`-raised `ValueError`s land inside `ctx.error` as the raw exception object, which `json.dumps` can't serialize, so any 422 from a custom validator (e.g. `PositionRateCreateRequest`'s "need at least one of incentive_pct/flat_ref_amount") crashed as an unhandled 500 instead of returning 422. This was actually caught while finishing Phase 2 testing, not Phase 3, but is recorded here since Phase 3's `KpiTemplateAssignmentCreateRequest`/criteria validators exercise the same code path — fixed via a `_sanitize_errors` pass that stringifies `ctx.error` before `jsonable_encoder`.
- 2026-07-20: Autogenerate's first pass at the migration proposed *dropping* `ix_employee_salaries_employee_id` and `ix_position_rates_position_id` — a false positive from a pre-existing drift between those Phase 2 indexes (created directly in the migration) and their ORM models (never declared `index=True`). Deliberately left both indexes alone and documented the drift inline in the new migration rather than silently including an unrelated DROP INDEX. Also caught and fixed a naming-convention doubling bug of my own: giving `CheckConstraint` an already-fully-qualified `name="ck_kpi_criteria_..."` gets the `ck_%(table_name)s_%(constraint_name)s` convention applied *again* on top, producing `ck_kpi_criteria_ck_kpi_criteria_...` — the fix is to name it just `"max_marks_positive"` and let the convention build the full name.
- 2026-07-20: DEMO GATE walked live end-to-end through the frontend's own dev proxy (not the backend's published port), as a real PMO test user: listed all 4 seeded templates (all pre-activated, 100/100) → opened SKILLED (v1, active, 30/20/10/10/10/10/10) → cloned to v2 (draft) → changed Production Efficiency 20→25 and Work Quality 10→5 (kept the 100 total) → activated v2 → confirmed the template's `active_version` flipped to v2 → **fetched v1 directly and confirmed its criteria are still 20/10 respectively — completely unchanged** → confirmed v1 (now archived) rejects a criteria edit with `version_not_draft` → confirmed a plain-HR-only account gets 403 creating a template but a PMO account succeeds, matching the PMO/ADMIN-only write rule. One live RBAC check for HR-assigning-a-template initially came back 403 unexpectedly; traced it to a shell scripting mistake in my own verification script (accidentally reused the reserved bash variable `$UID`, so the "assign HR role" call silently hit the wrong user id and the test account ended up roleless) — not an application defect. The PMO/HR assignment-write path is already covered correctly by the automated test suite and is structurally identical to the already-verified Phase 2 rate/salary write-permission pattern.
- 2026-07-20: All quality gates green inside the actual containers: backend `ruff check`, `mypy app` (44 source files), `pytest` (57/57), frontend `tsc -b`, `eslint`, `vitest run`, `vite build`, and the repo line-limit script.

## Phase 4 — Attendance Upload   [status: done]   (2026-07-20)
- [x] `incentive_periods`, `attendance_imports`, `attendance_records`, `attendance_zero_flags` models + migration `754b32c3632c`
- [x] `attendance/importer.py` — pure (bytes in, rows+issues out, zero DB access), validates sheet name + all 17 headers verbatim (fail-fast on drift), row `Month` must equal the declared period, normalizes Person No., bucket-sum sanity check (Present+Off Days+Absent+Leave+Public Holiday vs days-in-month) as a warning not an error. Column types verified against the real 420-row file first (not assumed): day-count columns are plain integers, `deduct_min`/`over_time`/`approved_over_time` are the only fractional ones.
- [x] dry-run → commit flow: same parser reused for both so the previewed parse is exactly what gets written; sha256-per-period idempotency (409 on an identical re-upload); superseding the previous active import on a genuinely different file; locked-period rejection (409); commit refuses to write anything if the file has parse errors (400, nothing persisted) — verified all of this against the real file, not just synthetic fixtures.
- [x] `zero_rule.py` — pure rolling leave+absence vs. contract allowance (45d/1yr, 90d/2yr), returns `None` (never guesses) when `contract_years` is missing, matching the known real-data gap from Phase 2. Wired into `commit_import`: recomputes for every matched employee over their trailing ≤6 existing periods. HR override endpoint (`POST /attendance/zero-flags/{id}/override`) with a required reason, audited.
- [x] Frontend (`attendance` module): periods list + create, a 3-step upload wizard (drag-and-drop → dry-run preview with a matched/unmatched summary banner and a severity-tagged issues table → commit, disabled while there are file-level errors), a records browse tab and an import-history tab per period, period lock/unlock, and a standalone zero-flags screen with override.
- [ ] OPEN QUESTION **not** answered (correctly, by design): the exact attendance-factor formula is a PMO decision explicitly deferred to Phase 7's calculation engine per INSTRUCTIONS.md ("implement as configurable rule, default = manual entry on the run line") — nothing in Phase 4 needed it, so it wasn't guessed at here.
- [x] DEMO GATE: **run live** against the real 420-row file — see notes.
### Notes / deviations
- 2026-07-20: **Real bug caught by the new tests, fixed**: the shared dry-run/commit upload endpoint never set `status_code=201` for the commit branch (only the `response_model` differed between the two branches), so a successful commit was silently returning `200` instead of `201`. Fixed by injecting `Response` and setting `response.status_code = 201` only on the commit path — dry-run stays `200`. Caught by 4 failing assertions in `test_attendance.py` before this was ever run live.
- 2026-07-20: All static/automated gates green inside the actual containers: `ruff`, `mypy` (50 source files), `pytest` (82/82 — 25 new: 9 parser + 6 zero-rule + 10 API-level), frontend `tsc -b`, `eslint`, `vitest run`, `vite build`, line-limit script.
- 2026-07-20: DEMO GATE walked live end-to-end through the frontend's own dev proxy with the real `docs/source/attendance_export_06-2026.xlsx`: created the June-2026 period → dry-run preview reported **420 total rows, 407 matched, 13 unmatched, zero issues** (bucket sums and Person No.s in the real file are all clean — no warnings even) → committed → **407 real `attendance_records` rows** browsable, correctly linked to the same employees enriched with `full_name_en` back in Phase 2 (same 407/13 split both times — a strong cross-phase consistency check, not a coincidence) → **re-uploading the identical file returned 409 `duplicate_import`** → locking the period and attempting a fresh upload returned 409 `period_locked` (unlocked again afterward) → zero-flags list came back empty, correctly, since no real employee has `contract_years` set yet (the documented Phase 2 data gap) — the rule degraded gracefully exactly as designed rather than fabricating a flag or crashing.
- 2026-07-20: Extended `app/common/errors.py`'s `bad_request`/`conflict` helpers with an optional `details` parameter (previously only `AppError` itself supported it) so the commit endpoint could return the full list of parse errors alongside the 400 — small, backward-compatible addition, no existing caller affected.
- 2026-07-20: Added a `[[tool.mypy.overrides]]` entry for `openpyxl.*` (`ignore_missing_imports`) — this is the first time `app/` code (as opposed to `scripts/`, which isn't mypy-checked) imports `openpyxl` directly, since Phase 2's `import_legacy.py` lives outside `app/`.

## Phase 5 — Approvals Engine + Evaluations   [status: done]   (2026-07-20)
- [x] `common/workflow.py` — generic `TransitionTable`/`apply_transition`/`transition_history` engine, `(status, action) → {to, roles, guard}` as data, one shared `approval_actions` table (+ `notifications`, write-only in v1 — no reader UI yet, same deferral pattern as the audit log viewer, both land in Phase 9). Deliberately entity-agnostic: nothing in the engine mentions "evaluation" — transfers (Phase 6) and incentive runs (Phase 7) reuse it as-is.
- [x] `evaluations` + `evaluation_scores` models + migration `0bd3259e2bd4`. `kind` (regular|self_appraisal) shares one status column but two distinct transition tables in `evaluations/workflow.py`; `template_version_id` pinned at creation; `owner_user_id` generalizes "whoever may edit while draft/returned" across both kinds (assigned reviewer for regular, the employee's own account for self-appraisal); `row_version` optimistic lock.
- [x] REGULAR: draft→submitted→manager_approved with a return loop (guards: `is_owner`, `is_dept_manager_of_employee`). SELF: draft→submitted→pmo_reviewed→fm_approved, return loops from either review step back to `returned` (resubmit always re-enters PMO review, never skips to FM).
- [x] `scoring.py` (pure): `awarded_marks_for_input` (marks mode = raw value; scale_1_5 mode = rank/5×max_marks, the verified legacy mapping) + `summarize_scores` (`score_pct = max(Σawarded,0)/Σmax`, grade bands 0.90/0.60/0.40 from PLAN §3.1). Grade bands are hardcoded constants, not an `app_settings` table — that table isn't required by any phase's task list yet, and would have been speculative scope.
- [x] `suggestions.py` (pure): overtime `min(⌊approved_OT/unit_hours⌋×points_per_unit, max_marks)`, absence `max_marks − penalty×absent_days` (may go negative), forced 0 under an active zero-attendance flag (Phase 4's `attendance_zero_flags`, correctly cross-checked by calendar month, not by row id). Only pre-fills `marks`-mode criteria — the legacy `scale_1_5` templates have no auto-sourced criteria in the real seed data, so this never collides in practice.
- [x] Endpoints: create (resolves template + reviewer/self + attendance suggestions into a draft) and bulk-create per department (never silently skips — every un-created employee comes back with a reason: `no_template_assigned`, `no_owner_resolved`, `already_exists`), scoped list/get, `PATCH` (draft/returned-only, row_version 409), `submit`/`approve`/`return`/`review`, `GET /approvals/pending` (unified inbox), `GET /approvals/evaluation/{id}/history`.
- [x] Frontend: `EvaluationScoresForm` (shared by the detail page and the bulk-grid's edit modal — criteria table with guidance tooltips, live client-side TOTAL/100 preview computed with the same formula as the backend, suggestion hint + Apply button), `BulkEntryPage` ("My Team" grid — one row per evaluation, Edit opens a modal so only one row's inputs are ever mounted at once, sidestepping the "400 controlled inputs" perf trap the plan called out), `EvaluationDetailPage` (action buttons computed from role+status+kind+ownership, `ApprovalTimeline`, `ApprovalActionModal`), `ApprovalsInboxPage`.
- [x] Tests (131 total, 49 new): exhaustive-enough transition matrix (invalid action, wrong role, cross-department guard failure, both kinds' full happy paths including the PMO-can't-be-bypassed-by-reviewer check), template-pinning reproducibility (clone+reweight+activate a v2 while an evaluation is pinned to v1 — v1's snapshot on the evaluation is provably untouched), optimistic-lock 409, suggestion math (unit tests + one integration test wiring a real `AttendanceRecord`), full workflow walks, scope enforcement (dept_manager/owner/HR visibility), generic workflow-engine tests using a `FakeEntity` (proves the engine has zero evaluation-specific coupling).
- [x] DEMO GATE: **run live** against real Phase 2–4 data — see notes.
### Notes / deviations
- 2026-07-20: **Real bug caught by my own tests before the demo, fixed**: my first pass at `test_evaluations.py` used the assigned *reviewer* to call `POST /evaluations` in nearly every test, but creation is correctly gated to HR/PMO/ADMIN (an administrative "kick off the cycle" action) — that's a deliberate design choice, not a bug, but it meant ~20 of my own tests would have failed with 403 had I not caught the mismatch while writing them. Fixed by routing all test setup through a `create_evaluation()` helper that always acts as HR, while still using the reviewer/manager accounts for the actions that are actually theirs (score, submit, approve, return).
- 2026-07-20: Also caught mid-write: a fixture-design bug (not an app bug) where building two test fixtures in one test both tried to create the *same* calendar period (`2026-06`), tripping the app's own correct `(year, month)` unique constraint. Fixed by having the test fixture look up-or-create the period, mirroring `get_or_create_period` in the real service — periods are genuinely global per month, not per-department, so two fixtures sharing one period is the realistic case anyway.
- 2026-07-20: There is no `users.employee_id` write path anywhere in the API (Phase 1 never added one — `POST/PATCH /users` only ever supported `staff_no`/`password`/`is_active`). Not a Phase 5 problem to fix, but it meant linking the live demo's dept-manager test account to its employee record needed a direct DB write (same class of action as the Phase 2 admin-password reset). Worth a real fix in a future phase if HR needs to link existing staff accounts to employee records through the UI.
- 2026-07-20: All gates green inside the containers: `ruff`, `mypy` (58 source files), `pytest` (131/131), frontend `tsc -b`, `eslint` (one Fast-Refresh warning fixed by moving a non-component export to its own file), `vitest run`, `vite build`, line-limit script.
- 2026-07-20: DEMO GATE walked live end-to-end on real Phase 2–4 data, not synthetic fixtures: assigned the real `LEGACY_TEAM` template (8 criteria, scale_1_5, the actual legacy scheme) to the real `fabricator` position → assigned a reviewer to 3 real Production-department Fabricators → bulk-created evaluations for the whole Production department (222 real employees): **3 created, 222 correctly skipped and explained** (202 `no_template_assigned` — only Fabricator got a template — and 20 `no_owner_resolved`), proving the "never silently skip" rule holds against genuinely messy real data, not a clean fixture → reviewer scored one crew member 4/5 on all 8 criteria (80%, grade B) and submitted → dept manager returned it with a comment about the Quality Control score → reviewer dropped Quality Control to 3/5 (recomputed live to 76%, grade B, row_version incremented) and resubmitted → dept manager approved → `GET /approvals/evaluation/1/history` shows all 4 steps in order with correct actors, statuses, and the return comment preserved verbatim → confirmed both inboxes updated correctly afterward (reviewer's pending list dropped to the other 2 still-draft evaluations, manager's pending list emptied).

## Phase 6 — Self-Appraisals + Transfers   [status: not started]
- [ ] self_appraisal flow (→PMO→FM) reusing renderer
- [ ] transfer_requests + apply-at-effective-date
- [ ] both in unified inbox
- [ ] DEMO GATE: transfer affects next month only

## Phase 7 — Calculation Engine + Incentive Runs   [status: not started]
- [ ] engine.py pure, Decimal, dual formula modes, round_step — TDD
- [ ] GOLDEN TESTS: recomputed March-2026 workbook row-for-row (validated with PMO)
- [ ] runs + line items (full snapshot) + lifecycle + period lock
- [ ] run dashboard + line breakdown + audit/approve screens
- [ ] OPEN QUESTIONS answered: position %s + salaries loaded (HR), pool source (PMO)
- [ ] DEMO GATE: draft→audit→approve→locked with drill-down

## Phase 8 — Reports & Exports   [status: not started]
- [ ] finance Excel export (openpyxl) + period summary
- [ ] WeasyPrint PDFs (vendored Arabic fonts, golden-file test)
- [ ] blank evaluation template generator (Excel + PDF)
- [ ] reports UI + downloads
- [ ] DEMO GATE: finance pack from locked run; Arabic PDF correct

## Phase 9 — Hardening & Deployment   [status: not started]
- [ ] audit viewer + dashboard widgets
- [ ] permissions penetration matrix (every endpoint × every role)
- [ ] i18n completeness + Arabic review by SAJCO staff
- [ ] backup/restore drill + deployment walkthrough
- [ ] UAT parallel month vs Excel — totals reconcile
- [ ] DEMO GATE: full month-cycle rehearsal on prod profile
